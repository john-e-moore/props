# dev_config.yaml

prefect:
  log_level: DEBUG
  retries: 2
  retry_delay_seconds: 30
  log_prints: true
  agent:
    type: cloud

aws:
  region: us-east-2
  s3_bucket: mushroomsundays
  s3_key: props/dev
  
duckdb:
  #db_path: data/warehouse/dev_warehouse.duckdb
  db_name: dev_warehouse.duckdb
  db_path: /mnt/c/Users/John/Documents/Personal/props

logging:
  log_level: DEBUG
